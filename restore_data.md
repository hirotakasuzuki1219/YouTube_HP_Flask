# データベースの復元方法

## 問題

Koyebでコンテナが再起動されると、SQLiteデータベースがリセットされ、ピン（マーカー）が消えてしまいます。

## 解決方法

### 方法1: 初期データを復元する（推奨）

`init_db.py`を使って初期データを復元できます。

#### ローカルで実行

```bash
python init_db.py
```

#### Koyebで実行（SSH接続が必要）

1. Koyeb Dashboardでアプリを選択
2. 「Settings」→「SSH」でSSH接続を有効化
3. SSH接続して以下を実行：

```bash
python init_db.py
```

### 方法2: 管理画面から手動で追加

1. アプリにアクセス
2. `/admin`にアクセス（ログインが必要）
3. 各ピンを手動で追加

### 方法3: データベースの永続化（本番環境推奨）

Koyebの無料版では、SQLiteデータベースは一時的なものです。本番環境では、以下のいずれかを使用することを推奨します：

1. **PostgreSQL**（Koyebのデータベースサービス）
2. **外部データベースサービス**（Supabase、PlanetScaleなど）
3. **定期的なバックアップ**

## 初期データの内容

`init_db.py`には以下のピンが含まれています：

- 成田国際空港
- エジプト・ギザ周辺
- 大エジプト博物館
- カイロ観光
- カイロ脱出
- カイロ国際空港
- イスタンブール
- アヤソフィアなど
- ザビハ・ギョクチェン国際空港
- カッパドキア
- ローズバレー

## 今後の対策

1. **定期的なバックアップ**: データベースを定期的にエクスポート
2. **外部データベースの使用**: PostgreSQLなどの永続的なデータベースを使用
3. **データのエクスポート機能**: 管理画面からデータをエクスポートできる機能を追加


